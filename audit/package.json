{
  "name": "gc-visualizer-audit",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Workflow de auditoría de calidad de código del proyecto gc-visualizer. Aislado del proyecto principal — sus dependencias y configuración no contaminan el código de producto.",
  "scripts": {
    "audit:all": "node scripts/audit.mjs",
    "audit:phase2": "node scripts/audit-phase2.mjs",
    "audit:phase3": "node scripts/audit-phase3.mjs",
    "audit:all-phases": "npm run audit:all && npm run audit:phase2 && npm run audit:phase3"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^9.6.1",
    "@stryker-mutator/jest-runner": "^9.6.1",
    "@stryker-mutator/typescript-checker": "^9.6.1",
    "depcheck": "^1.4.7",
    "eslint-plugin-jest": "^29.15.2",
    "jscpd": "^4.2.3",
    "knip": "^6.14.1",
    "madge": "^8.0.0"
  }
}
